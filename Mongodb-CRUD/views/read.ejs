<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link rel="stylesheet" href="/stylesheets/app.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full h-[100vh] bg-zinc-800 p-10 text-white">
      <a class="text-blue-400" href="/">Go To Home</a>
      <h1 class="text-2xl tracking-tight font-semibold mb-3 mt-10">
        All Users
      </h1>
      <div class="main flex gap-3 flex-wrap">
        <% if(users.length > 0){ %> <% users.forEach((user)=>{ %>
        <div class="user w-72 bg-zinc-600 text-white p-2 rounded-lg">
          <div class="w-full h-52 bg-zinc-700 rounded-xl overflow-hidden">
            <img
              src="<%= user.image %>"
              alt="user image"
              class="w-full h-full object-cover object-top"
            />
          </div>
          <h1 class="text-2xl tracking-tighter"><%= user.name%></h1>
          <p class="text-zinc-300 mb-4"><%= user.email %></p>
          <a href="/edit/<%= user._id %>" class="text-white">Edit User</a>
          <a href="/delete/<%= user._id  %>" class="text-red-400"
            >Delete User</a
          >
        </div>
        <% }) %> <% } else{ %>
        <div class="text-2xl text-zinc-300 tracking-tight">No Users yet...</div>
        <% } %>
      </div>
    </div>
  </body>
</html>
