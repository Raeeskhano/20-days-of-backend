<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="w-full flex justify-end">
        <a
          class="py-2 px-3 bg-red-500 rounded-md mb-5 inline-block text-sm"
          href="./logout"
          >logout</a
        >
      </div>
      <h4 class="mb-2">Edit your post.</h4>

      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="w-1/3 border-2 border-zinc-700 rounded-md p-3 outline-none block"
          placeholder="what's in your mind?"
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          class="px-3 py-2 bg-blue-400 rounded-md mt-2 w-40 text-sm"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
