<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
      <div class="w-full flex justify-end">
        <a
          class="py-2 px-3 bg-red-500 rounded-md mb-5 inline-block text-sm"
          href="./logout"
          >logout</a
        >
      </div>
      <h1 class="text-2xl tracking-tighter">Hello, <%= user.name %>👋</h1>
      <h4 class="mb-2">you can create a new post.</h4>

      <form action="/post" method="post">
        <textarea
          class="w-1/3 border-2 border-zinc-700 rounded-md p-3 outline-none block"
          placeholder="what's in your mind?"
          name="content"
        ></textarea>
        <input
          class="px-3 py-2 bg-blue-400 rounded-md mt-2 w-40 text-sm"
          type="submit"
          value="Create New Post"
        />
      </form>

      <div class="mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="mt-5 flex gap-4 flex-wrap">
          <% user.posts.reverse().forEach((post)=>{ %>
          <div
            class="w-1/3 p-4 rounded-md border-2 border-zinc-700 bg-zinc-800"
          >
            <h4 class="mb-2 text-blue-400"><%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>

            <small class="mt-4 inline-block"></small>
              <%= post.likes?.length || 0 %> likes</small
            >
            <div class="flex gap-4 mt-2 text-sm">
              <a class="text-blue-400" href="/like/<%= post._id %>"><%= post.likes.indexOf(user._id) === -1 ? "Like" : "UnLike" %></a>
              <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
